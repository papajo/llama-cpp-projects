[project]
name = "prompt-cache-chunking"
version = "0.1.0"
description = "Compare chunking strategies for prompt cache efficiency and retrieval quality"
requires-python = ">=3.10"
dependencies = [
    "numpy>=1.24",
]

[project.optional-dependencies]
test = ["pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
