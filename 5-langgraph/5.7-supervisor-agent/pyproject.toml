[project]
name = "supervisor-agent"
version = "0.1.0"
description = "Supervisor agent — hierarchical delegation to sub-agents"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
test = ["pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
