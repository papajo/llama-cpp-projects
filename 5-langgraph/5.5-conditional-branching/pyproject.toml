[project]
name = "conditional-branching"
version = "0.1.0"
description = "Agent workflow with conditional branching — route execution along different paths"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
test = ["pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
